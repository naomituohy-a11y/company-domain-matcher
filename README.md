# Company–Domain–Email Matching

Upload a spreadsheet, choose the **Company** and **Domain** columns,  
and download an enriched `.xlsx` with validation + confidence.

Run locally:
```bash
pip install -r requirements.txt
python app.py
